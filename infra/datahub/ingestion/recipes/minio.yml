source:
  type: s3
  config:
    path_specs:
      - include: "s3://tuantienti/*"

    aws_config:
      aws_access_key_id: minio
      aws_secret_access_key: minio123
      aws_region: us-east-1
      aws_endpoint_url: "http://192.168.26.181:9000"

    env: PROD

sink:
  type: datahub-rest
  config:
    server: "http://datahub-gms:8080"
